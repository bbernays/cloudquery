variables:
  component: source/openapi
  binary: openapi

project_name: plugins/source/openapi

monorepo:
  tag_prefix: plugins-source-openapi-
  dir: plugins/source/openapi

includes:
  - from_file:
      # Relative to the directory Go Releaser is run from (which is the root of the repository)
      path: ./plugins/.goreleaser.yaml