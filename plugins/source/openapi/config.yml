kind: source
spec:
  # Source spec section
  name: aws
  registry: grpc
  path: localhost:7777
  # path: cloudquery/aws
  # version: v15.6.0
  destinations: [ "postgresql"]
  # skip_tables: ["aws_ec2_vpc_endpoint_service_configurations"] 
  # concurrency: 100
  spec: 
    open_api_url: https://api.snyk.io/rest/openapi/2021-11-03~beta


---
kind: destination
spec:
  # pk_mode: cq-id-only
  name: "postgresql"
  registry: "github"
  path: "cloudquery/postgresql"
  version: "v3.0.3"
  # registry: grpc
  # path: localhost:7778
  migrate_mode: forced
  spec:
    connection_string: "postgresql://postgres:pass@localhost:5432/postgres?sslmode=disable"






